# Archived Code

This folder contains transition or obsolete scripts kept for traceability.

The final dataset construction and parser evaluation flow uses the scripts directly under `Code/`, not this archive.

Archived scripts may still contain historical absolute paths or old dataset names.
They are retained only as development records and are not required to reproduce
the final results.
